1.How to push the folder for local repository to remote repository ?
A. We have to open file manager in that file manager we have to open the folder of right click then get the options 
   like git bash open here click the option *git add *git status *git commit -m "___ " *git push 

2. git restore :- Restore specified paths in the working tree with some contents from a restore source. if the path is tracked 
   but does not exist in the restore source, it will be removed to match the source.

command :-git restore --staged (file name)

3. git revert :-  This creates a new commit that undoes the changes of the specified commit.

command :-git revert 'commit id'
It given one or more existing commits, revert the changes that the related patches introduce, and record new commits that
record them. this requires your working tree to be clean (no modification from the head commit)

4.Fork :- A fork is a copy of repository forking a repository allows you to freely experiment with changes without effecting the
          original project.
          by default, forks are named  with same their upstream repository.

5.git merge :-join two or more development histories together. In corporate changes from named commits. (since the time their histories
              diverged from the current branch)into the current branch this command is used by git pull to incorporate changes from
              another repository can be used by hand to merge changes from one branch into another.

"PR" :- (pull request)when merging PR, you can allow any combination of merge commit, squashing or rebasing . at least one option 
        must be e1nabled. If you have linear history requirement enable on any protected branch, you must enabled squashing or rebasing.

Squash :- combine as commits from head branch into single commit into a base branch 

*git commit of -am :- It works two steps commit and add like committing and staging. its particularly  existing file only.